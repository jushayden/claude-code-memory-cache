# ── Secrets — NEVER commit ─────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem
secrets/
**/apikey*
**/api_key*

# ── Personal data stores — NEVER commit ────────────────
# Your actual Obsidian vault and memory stores are private.
vault/
Vault/
ObsidianVault/
data/
chromadb*/
*.chromadb/
memory/            # per-project fact files are personal; ship examples only
hook_state.json

# Your own config, made by copying memory_server/config.example.py.
# It holds the real path to your vault, so it must never be committed.
memory_server/config.py
config.py
!memory_server/config.example.py

# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Node ───────────────────────────────────────────────
node_modules/

# ── Generated graph artifacts ──────────────────────────
graphify-out/
.code-review-graph/

# ── OS / editor ────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# machine-local hook artifacts
PROJECT_BRAIN.md
graphify-out/
.code-review-graph/
